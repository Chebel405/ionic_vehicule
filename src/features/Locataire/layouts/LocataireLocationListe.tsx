import { Locataire } from '../Locataire';



interface Props {
    location: Location;
    locataire: Locataire;
}

export const LocataireLocationListe = (props: Props) => {
    return <IonList>
        {
            props.locataire ?
        }
    </IonList>
}